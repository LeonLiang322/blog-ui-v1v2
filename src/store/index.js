import { createStore } from 'vuex'
import snackbar from "@/store/modules/snackbar";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    snackbar
  }
})
